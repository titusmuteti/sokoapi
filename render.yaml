databases:
  - name: sokodb
    databaseName: sokodb
    user: sokodb_user

services:
  - type: web
    name: sokoapi
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        value: "postgres://sokodb_user:dbBTboIjuB8ByGiGU0Vc6oWarfqOIFOZ@dpg-clfmbid8td7s73bb9bvg-a/sokodb"
      - key: RAILS_MASTER_KEY
        sync: false
